# LoveTalk

Minimal instructions

Run the development server:

```bash
node server.js
```

Notes:
- The project uses ES modules ("type": "module" in `package.json`) so `import` syntax is used in `server.js`.
